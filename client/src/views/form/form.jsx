import Formulario from "../../components/Formulario/Formulario";

const Form = () => {
    return (
        <div>
        <h1>Esta es la vista del Form</h1>

        <Formulario />
        </div>
    )
};

export default Form;